# Copy to .env (not committed)
DATABASE_URL=postgresql+psycopg2://tp_user:tp_pass@localhost:5433/tp_variant_03

# Optional: LLM API (week 14). If empty, template summary is used.
OPENAI_API_KEY=
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
